# Web framework
fastapi
uvicorn
pydantic
pydantic-settings
python-multipart
python-dotenv
aiofiles

# Database
SQLAlchemy>=2.0
asyncpg
psycopg2-binary
pgvector
alembic

# AI / ML
easyocr
pytesseract
mediapipe==0.10.21
transformers
torch>=2.4
torchvision
xgboost
shap
opencv-python
Pillow
numpy<2
pandas

# FaceNet. facenet-pytorch 2.6.0 declares torch<2.3, which conflicts with
# transformers (torch>=2.4), so it is intentionally NOT listed here. Install it
# without touching torch:
#     pip install --no-deps facenet-pytorch==2.6.0

# Security
cryptography
python-jose[cryptography]

# Testing
pytest
pytest-asyncio
httpx
